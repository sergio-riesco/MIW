#cloud-config
package_update: true
ssh_pwauth: true

users:
  - name: 'user'
    plain_text_passwd: '1234'
    groups:
      - sudo
    lock_passwd: false
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash

packages:
    - docker.io
    - python3-flask

write_files:
   - path: '/home/user/app.py'
     permissions: '0644'
     owner: root:root
     content: |
       from flask import Flask
       app = Flask(__name__)
       @app.route('/')
       def hello():
           return '<h1>Hola Mundo - VM1 de User</h1>'
       if __name__ == '__main__':
           app.run(host='0.0.0.0', port=80)

runcmd:
    - chown user:user /home/user/app.py
    - nohup python3 /home/user/app.py > /home/user/app.log 2>&1 &